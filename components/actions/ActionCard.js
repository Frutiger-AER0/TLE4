import {SafeAreaView} from "react-native-safe-area-context";
import {View} from "react-native";

export default function ActionCard() {
    return (
        <SafeAreaView>
            <View className="bg-lightPurple rounded-xl border p-5 flex flex-col gap-4">

            </View>
        </SafeAreaView>
    );
}